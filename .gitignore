# Xcode
build/
DerivedData/
*.xcworkspace
*.xcuserdata/
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/

# Dependencies
Pods/

# Local secrets
.env

# OS
.DS_Store

# Raw marketing/screenshot captures — commit only the README-referenced
# subset via `git add -f screenshots/<file>`. Tracked app-icon PNGs under
# EscapeMint/App/Assets.xcassets are unaffected (already committed).
screenshots/*.png
screenshots/ipad/

# Editor/terminal themes
.ghostty-theme
